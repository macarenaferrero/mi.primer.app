import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(


/*   TODO: Que muestre diferentes componenetes que serian paginas.

  1. Pagina INICIO
  2. PAgina SERVICIOS
  3. PAgina QUIENES SOMOS
  4. Pagina CONTACTO

  <BrowserRouter>
            <Navbar/>
            <Switch>
               <Route path="/" component={() => <App />} />
               <Route path="/" component={() => <Servicios />} />
               <Route path="/" component={() => <Quienes Somos />} />
               <Route path="/" component={() => <Contacto />} />
            </Switch>
            </BrowserRouter> 
   */
  





  <React.StrictMode>
    <App />
  </React.StrictMode>
);
